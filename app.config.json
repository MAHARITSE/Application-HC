{
  "app": {
    "name": "HC-Manager",
    "host": "127.0.0.1",
    "port": 3000,
    "mode": "dev",
    "openBrowser": true
  },
  "database": {
    "mode": "json-only",
    "note": "Utilisation de fichiers JSON uniquement (data/*.json). PostgreSQL désactivé."
  },
  "setup": {
    "installDependencies": true,
    "createEnvFile": false,
    "syncDrizzleConfig": true,
    "pushDatabaseSchema": true
  }
}
